int add(a, b)
{
	return (a + b);
}
int sub(a, b)
{
	return (a - b);
}
int mul(a, b)
{
	return (a * b);
}
int div(a, b)
{
	if (b != 0)
	{
		return (a / b);
	}
	return (0);
}
int mod(a, b)
{
	if (b != 0)
	{
		return (a % b);
	}
	return (0);
}
